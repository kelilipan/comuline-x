export interface UIState {
  showSearch: boolean;
  showFilter: boolean;
  showAddStation: boolean;
}
export const INITIAL_UI_STATE = {
  showSearch: false,
  showFilter: false,
  showAddStation: false,
};
